services:
  notes-app:
    build:
      context: .
      dockerfile: Dockerfile
    image: notes-app:latest
    container_name: notes-app
    ports:
      - "3000:3000"
    restart: unless-stopped
    environment:
      NODE_ENV: production
      HOST: 0.0.0.0
      PORT: 3000