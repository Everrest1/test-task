## Стек

- Nuxt 4 (`ssr: false`), Vue 3, Pinia, TypeScript
- SCSS
- Vitest
- Docker.

## Запуск

```bash
npm install
npm run dev          # http://localhost:3000
```

## Docker

```bash
docker compose up --build
# приложение будет доступно на http://localhost:3000
```